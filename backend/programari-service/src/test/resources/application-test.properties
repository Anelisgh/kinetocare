spring.application.name=programari-service
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/programari_test_db?createDatabaseIfNotExist=true
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:Emma19}
spring.jpa.hibernate.ddl-auto=update

app.service-names.initial=Evaluare Initiala
app.service-names.recurring=Reevaluare

# Dummy JWK Set URI to prevent startup validation issues
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/realms/kinetocare/protocol/openid-connect/certs

spring.rabbitmq.listener.simple.auto-startup=false
